# Аватарки голосов

Структура: `провайдер/voice_id/voice.json`.

Положите в папку голоса одно изображение с любым названием. Поддерживаются `.png`, `.jpg`, `.jpeg`, `.webp`, `.gif`. Если изображений несколько, файл с названием `avatar` используется первым, иначе выбирается первый файл по имени.
